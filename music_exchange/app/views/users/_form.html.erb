<%= semantic_form_for(@user) do |f| %>
  <%= f.inputs do %>
    <%=f.inputs :email, :name, :surname, :username, :about %>
  <% end %>
  <%= f.actions do %>
    <%= f.action :submit, :as => :button %>
  <% end %>
<% end %>